<?php
    /*try
                {
                	// On se connecte à MySQL
                        $bdd = new PDO('mysql:host=localhost;dbname=photoforyoubureau;charset=utf8', 'root', '');
                }
                catch(Exception $e)
                {
                	// En cas d'erreur, on affiche un message et on arrête tout
                        die('Erreur : '.$e->getMessage());
                }
                $req = "INSERT INTO `photoforyoubureau`.`photos` (`titre`, `description`, `source`, `pseudoUser`, `tailleX`, `tailleY`) VALUES (".$_POST['titre'].", '".$_POST['description']."', ".$target_file.", ".$_SESSION['pseudo'].", 5000,5000);";
                $reponse = $bdd->query($req);  

                // On affiche chaque entrée une à une
                while ($data = $reponse->fetch())
                {
                    echo "</br><img src=".$data['source']." alt='Smiley face' height='151' width='235' >"."</br>".$data['titre']."</br> ".$data['description']."</br>";
                }

                $reponse->closeCursor(); // Termine le traitement de la requête
*/
?>

